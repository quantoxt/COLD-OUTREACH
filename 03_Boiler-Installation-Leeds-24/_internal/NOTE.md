# NOTE — skipped by decision

The live website is okay and needs no rebuild. This project is intentionally
skipped — do not rebuild a landing page for it.

- Scraped copy/links are preserved in `_internal/resources/` for reference.
- The site's images are all inline `data:` placeholders (lazy-load shimmer),
  so there is nothing downloadable; nothing is missing.
- No further action required.
